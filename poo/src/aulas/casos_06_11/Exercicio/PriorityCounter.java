package casos_06_11.Exercicio;


public class PriorityCounter extends Counter {
    // Caixa especial que pode atender clientes prioritários

    // Construtor da classe que herda de Counter
    public PriorityCounter(int number) {
        super(number);
    }
}
